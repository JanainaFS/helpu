{{#each erros}}
<div class="alert alert-danger"> {{texto}}</div>
{{else}}
{{/each}}
<h3> Novo setor: </h3>
<div class="card">
  <div class="card-body">
    <form action="/setor/novo" method="POST">
      <div class="form-group">
        <label for="nome"> Nome: </label>
        <input type="text" id="nome" name="nome" placeholder="Nome do setor" class="form-control">
      </div>
      <button type="submit" class="btn btn-success"> Criar setor </button>
    </form>
  </div>
</div>